org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
# Pub cache (C:) and project (D:) live on different drives; Kotlin's incremental
# compiler can't compute a relative path across Windows drive roots and crashes
# ("this and base files have different roots"). Disable incremental Kotlin
# compilation so the release build succeeds. Costs a little rebuild speed only.
kotlin.incremental=false
